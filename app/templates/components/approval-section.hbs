{{#if build.isFinished}}
  <div class="NavMenu-item NavMenu-item--right">
    {{approval-button build=build approve=(action "approve")}}
  </div>
  {{#if build.isApproved}}
    <div class="NavMenu-item NavMenu-item--right NavMenu-item--text">
        <strong>Approved</strong>
    </div>
  {{/if}}
{{/if}}
